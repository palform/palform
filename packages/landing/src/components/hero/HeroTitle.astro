---
interface Props {
    center?: boolean;
}

const { center } = Astro.props;
---

<h1
    class:list={[
        "xl:text-7xl lg:text-6xl text-5xl font-display leading-none tracking-tight text-gray-900 dark:text-gray-100",
        center ? "text-center" : "",
    ]}
>
    <slot />
</h1>
