---
const { class: className } = Astro.props;
---

<div
    class:list={[
        className,
        "w-full py-8 px-8 md:px-12 rounded-3xl bg-gradient-to-r from-primary-500 to-purple-600 dark:from-primary-700 dark:to-purple-900 text-left text-white",
    ]}
>
    <div class="flex justify-between flex-col md:flex-row">
        <div class="w-full md:w-1/3 mr-4">
            <h1 class="font-display text-6xl font-medium">Free forever</h1>
            <p class="mt-2">
                That's right: we'll <em>never</em> charge for these features. It
                hardly adds to our expenses, so why should we?
            </p>
            <p class="mt-2">No card details required!</p>
        </div>
        <div class="flex flex-col md:flex-row md:gap-8 mt-6 md:mt-2">
            <ul class="text-2xl font-display">
                <li>Unlimited responses</li>
                <li>10 forms</li>
                <li>20 questions/form</li>
                <li>1 user</li>
                <li>All question types</li>
            </ul>
            <ul class="text-2xl font-display">
                <li>Hidden fields</li>
                <li>E-signatures</li>
                <li>Unlimited file uploads</li>
                <li>Conditional branching</li>
                <li>Email + webhook</li>
            </ul>
        </div>
    </div>
</div>
